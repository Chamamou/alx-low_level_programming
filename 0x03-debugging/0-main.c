#include "main.h"
/**
 * main - Testing the function if it is positive or negative
 * Return: 0
 */

int main(void)
{
	void positive_or_negative(int i);
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}

